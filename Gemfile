source 'https://rubygems.org'

# Docs toolchain only — validates the AsciiDoc manual (`just docs-check`).
# No PDF/EPUB generation (T-047, decision 1).
gem 'asciidoctor', '~> 2.0'
gem 'logger' # no longer a Ruby default gem; asciidoctor requires it
