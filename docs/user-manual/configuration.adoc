[#configuration]
= Configuration — `pickle.toml`

`pickle.toml` lives at the overarching-project root and is *tool-managed*, but
only one command rewrites a file you already own:

* *`project add|remove`* re-render the whole file to a canonical layout. That
  re-render *does not preserve comments* — this is the one case where
  hand-written notes are lost.
* *`upgrade`* rewrites only the `payload_version` line. Comments, blank lines,
  key order and every other line's spacing survive; that one line comes back
  normalised to `payload_version = "value"`, with any inline comment after the
  value kept — so that one line's alignment and quoting style are not
  preserved. The line editor tracks multi-line strings and array nesting
  as it scans, so a quoted `"payload_version"` key and a table-looking or
  key-looking line inside an unrelated multi-line string are read correctly
  wherever they sit in the file — position is not what decides it. Two
  shapes are beyond a line-based edit by design: `payload_version`'s own
  value being a multi-line string or an array — pickle never writes either,
  so it only happens to a hand-edited file — and a file that does not parse
  to begin with (for example, a duplicated key). And because the editor is a
  bounded scanner rather than a full TOML parser, a sufficiently exotic file
  can still defeat it (a multi-line string that escapes its own closing
  delimiter, say). None of that can cost you content: the result is always
  parsed and compared against the original before anything is written, so
  whenever the editor cannot do the job `upgrade` *refuses and changes
  nothing* and tells you to set the version by hand, naming the offending
  line whenever it knows which one it is.
* *`install`* writes `pickle.toml` only when it does not yet exist; on an
  existing file it reports `= pickle.toml (exists)` and leaves it alone.

Hand-edits are always preserved on load.

== The keys

*Overarching:*

* `payload_version` — the payload pinned into this project (stamped by
  install/upgrade).
* `review_addendum` (optional) — a project-wide review addendum layered on the
  skill's review protocol.
* `[commit]` table — `overarching_auto`, `child_publish_gated`. Both default
  to *`true`* when the key or the whole table is absent — the cautious
  reading, and what `install` writes: a child is not pushed without your
  approval. Set either to `false` explicitly to opt out; the wording rendered
  into `AGENTS.md` follows whichever way they are set (see <<cmd-upgrade>>).

*Per child — the `\[[project]]` array* (one entry per connected child):

* `name`, `path` (relative to the root);
* `build` / `test` / `lint` / optional `docs` — the child's own commands, used
  by the agent when implementing and reviewing tickets that target it (this
  repository, for example, registers `docs = "just docs-check"` to validate
  this manual);
* `ticket_prefix` (default `T`) — the per-child id prefix (`<PREFIX>-NNN`);
* `branch_prefix` (default `feat/`);
* `wip_in_development` / `wip_in_review` (default 1);
* optional per-child `review_addendum`.

The shipped ticket-flow skill (`.agents/skills/ticket-flow/`) states only the
flow's *defaults* for the ticket-id prefix, the branch prefix, the WIP limits
and the commit policy, deferring to the project's actual configuration. The
`AGENTS.md` marker block is the authoritative render of what is actually
configured: after changing `ticket_prefix`, `branch_prefix`,
`wip_in_development`/`wip_in_review` or `[commit]`, re-run `pickle upgrade` (or
`pickle project add`, which re-renders it directly) and read the marker block
— not the skill — for this project's real values.

== Parsing

TOML is decoded with
https://github.com/BurntSushi/toml[`github.com/BurntSushi/toml`] — a
*build-time* dependency compiled into the static binary; nothing is fetched at
runtime.
